<!DOCTYPE html>
<html>
    <%= render 'shared/head' %>
  <body>
    <%= render 'shared/navbar' %>
    <div class="<%= body_class %>">
      <div class="content">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
