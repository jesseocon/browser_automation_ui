<%= form_with(model: test_suite, local: true) do |form| %>
  <% if test_suite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(test_suite.errors.count, "error") %> prohibited this test_suite from being saved:</h2>

      <ul>
      <% test_suite.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :base_url %>
    <%= form.text_field :base_url %>
  </div>

  <div class="field">
    <%= form.label :viewport %>
    <%= form.number_field :viewport %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
